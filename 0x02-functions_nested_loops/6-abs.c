#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *  @n: the integer that helps check 
 * Returns the absolute value _abs
 */

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
