#include "main.h"

/**
 * _isalpha - help checks for alphabets @c
 * Returns: 1 if c is a letter else 0 otherwise
 */

int _isalpha(int c)
{
return ((c >= 'a' && c <= 'z') || (c >= 'A' || c <= 'Z'));
}
