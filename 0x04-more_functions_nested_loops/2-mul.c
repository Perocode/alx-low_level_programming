#include "main.h"

/**
 * mul - helps in multiplication of two integers
 * @a: int to be multiplied to b
 * @b: int to be multiplied to a
 * Returns the result
 */

int mul(int a, int b)
{
int c;
c = a * b;
return (c);
}
