#include "main.h"
#include <stdio.h>

/**
 * main - prints the name of the program
 * @argc: count
 * @argv: vector
 * Return: int
 */

int main(int argc, char *argv[])
{
(void)argc;
printf("%s\n", argv[0]);
return (0);
}
