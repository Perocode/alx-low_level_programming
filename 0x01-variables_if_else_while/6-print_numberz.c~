#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Successful)
 */

int main(void)
{
  int a = 0;

  while (a < 10)
    {
     putchar(a%10 + '0');
      a++;
    }
  
  return (0);
}
