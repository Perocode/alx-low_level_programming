#include "lists.h"

/**
 * lisint_len - returns the number of elements in a linked list
 * @h: points to the elemnets on the list
 * Return: number of elements in the list
 */

size_t listint_len(const listint_t *h)
{
int c = 0;

while(h)
{
h = h->next;
c++;
}
return (c);
}
