#include "main.h"

/**
 * _memset - it helps fill the memory with constant
 * @s: char pointer to string
 * @b: constant byte
 * @n: maximum bytes to use
 * Return: pointer s  
 */

char *_memset(char *s, char b, unsigned int n)
{
unsigned int x;
for (x = 0; x < n; x++)
{
s[x] = b;
}
return (s);
}
