#include "main.h"

/**
 * _strlen - returns the length of the string
 * @s: pointer to strings
 * Return: the values of string
 */

int _strlen(char *s)
{
  int len;

  len = 0;

  while (*(s + len) != '\0')
    {
      len += 1;
    }
  return (len);
}
